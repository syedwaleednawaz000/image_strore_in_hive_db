name: store_image_in_local_database
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.17.6 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.5
  image_picker: ^0.8.5+3
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  isar: ^2.5.0
  isar_flutter_libs: ^2.5.0
  path_provider: ^2.0.11

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.1
  hive_generator: ^1.1.3
  isar_generator: any
  build_runner: any


flutter:
  uses-material-design: true
